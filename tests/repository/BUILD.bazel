load("//:defs/defs.bzl", "pytest_test")

pytest_test(
    name = "test_repository",
    srcs = glob(["test_*.py"]),
    deps = [
        "//cheeseshop/repository",
        "@pypi-test//pytest",
        "@pypi-test//pytest_vcr",
        "@pypi//loguru",
    ],
    # might need refreshing cassette, failing for now
    # data = ["//tests/repository/cassettes:cassettes"]
)

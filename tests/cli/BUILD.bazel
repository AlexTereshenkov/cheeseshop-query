load("//:defs/defs.bzl", "pytest_test")

pytest_test(
    name = "test_cli",
    srcs = ["test_cli.py"],
    deps = [
        "//cheeseshop/cli",
        "@pypi-test//pytest",
        "@pypi-test//pytest_vcr",
    ],
    # might need refreshing cassette, failing for now
    # data = ["//tests/cli/cassettes"],
)

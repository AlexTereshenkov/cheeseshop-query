load("@rules_python//python/entry_points:py_console_script_binary.bzl", "py_console_script_binary")

py_console_script_binary(
    name = "tabulate",
    pkg = "@pypi-tools//tabulate",
)

# Makes the file available to other packages via Bazel labels (e.g., //path/to:pytest_runner.py).
# This is necessary if you want to include the file in the srcs of a py_test in a different package.
# Declaring a py_binary for pytest_runner is optional unless you want to run it as a standalone binary;
# for use as a test runner, just exporting the file is sufficient
exports_files(["pytest_runner.py"])
